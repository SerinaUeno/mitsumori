# 設計原則: 0円運用アーキテクチャ（InspirationCat型）

## 基本方針

**大衆向けに公開する個人開発ツールは、デフォルトで「サーバーを持たない設計」を採用する。**

参考事例：InspirationCat（https://inspiration-cat.pages.dev/）
公開初日で約50万アクセスを 0円 で捌いた実例。作者談：「バズってから慌ててサーバを増強した、みたいな話ではない。そもそもサーバがない」。

## 原則

1. **サーバーを持たない** — 動的バックエンドは作らない。処理はすべてブラウザ内で完結させる
2. **ホスティングは Cloudflare Pages を第一選択** — 帯域無制限・無料。Vercel / Netlify は帯域課金のリスク（同規模で数万円〜数十万円請求の可能性）あり
3. **画像・個人データはサーバーに送らない** — プライバシー保護と保存コストゼロを同時に実現
4. **AI処理もクライアントで** — ONNX Runtime Web + WebAssembly / WebGPU をブラウザ内で実行。モデルは初回DLしてキャッシュ
5. **軽量スタックを優先** — Vite + vanilla TypeScript など、不要なフレームワークは入れない
6. **端末性能に応じたフォールバック** — 解像度や処理精度を自動で落として安定動作（例：1024px → 768px → 512px）
7. **外部CDN依存は最小限に** — Tailwind / Fonts のCDNは許容。外部API依存は原則避ける

## 採用ルール

- 新規ツールは、まずこの原則でスペックを組む
- **クライアントサイドで実現できない要件が出た場合は、トレードオフを明示してユーザーに確認してから** 判断する
- 「機能を削ってでもサーバー0円を守るか」「サーバーを持ってでも機能を優先するか」はユーザーが決める事項

## 技術選択チートシート

| 用途 | 推奨 |
|---|---|
| ホスティング | **Cloudflare Pages**（GitHub Pagesでも可だが帯域上限あり） |
| AI推論 | ONNX Runtime Web (WebAssembly / WebGPU) |
| 背景除去 | `@imgly/background-removal` |
| 画像編集 | Canvas API |
| 画像フォーマット | WebP優先 + PNGフォールバック |
| フレームワーク | Vite + vanilla TS を基準。必要な場合のみ React |
| 状態保存 | localStorage / IndexedDB（端末内のみ） |

## メリット再確認

- 💸 **コスト**：計算ゼロ + 保存ゼロ + 転送ゼロ の三拍子
- 🔒 **プライバシー**：画像やEXIFがサーバーを通らない → 運営側のセキュリティ対応が不要
- 📈 **バズ耐性**：スパイクに強く、サイトが落ちない・課金されない
- 😌 **運用負荷**：保守・監視がほぼ不要

## 本リポジトリの現状

`index.html`（真・納期ジェネレーター）は完全に静的（HTML + Tailwind CDN + vanilla JS）。
サーバー処理・データ保存・外部API呼び出しはいずれも無し。
→ **この原則に既に合致している。クオリティトレードオフなし。**

公開時は Cloudflare Pages に接続するのが最適。

## 参考リンク

- ITmedia NEWS の紹介記事
- 作者：電電猫猫 / Naokiさん（X: @nya3_neko2）
- Qiitaでの技術分析（キーワード：InspirationCat、ONNX Runtime Web、Cloudflare Pages）
